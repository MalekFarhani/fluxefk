apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: efk
  namespace: efk
spec:
  interval: 1m
  chart:
    spec:
      chart: ./fluentd
      sourceRef:
        kind: GitRepository
        name: efk
        namespace: efk