apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kibana
  namespace: efk
spec:
  interval: 1m
  chart:
    spec:
      chart: ./kibana
      sourceRef:
        kind: GitRepository
        name: efk
        namespace: efk